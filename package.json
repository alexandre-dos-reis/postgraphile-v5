{
  "name": "postgraphile-v5",
  "type": "module",
  "scripts": {
    "envvars": "eval export $(cat .env)",
    "migrate": "bun run envvars | DATABASE_URL=$POSTGRAPHILE_DB_URL SHADOW_DATABASE_URL=$GRAPHILE_MIGRATE_SHADOW_URL bunx graphile-migrate",
    "dev": "bun run envvars | bunx postgraphile -P postgraphile/presets/amber --watch --allow-explain --port $POSTGRAPHILE_PORT --connection $POSTGRAPHILE_DB_URL"
  },
  "devDependencies": {
    "bun-types": "latest",
    "graphile-migrate": "^1.4.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "postgraphile": "beta"
  }
}
